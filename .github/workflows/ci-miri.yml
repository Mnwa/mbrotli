name: CI Miri

# Run on demand from the Actions tab or the GitHub CLI.
on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  miri:
    name: interpreter checks for retained storage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@nightly
        with:
          components: miri
      # Filters name modules rather than single tests: a renamed test would
      # leave a single-name filter matching nothing, and `cargo miri test`
      # exits successfully when it runs no tests at all.
      - run: cargo miri test --lib compressor::core::shared::ringbuffer::tests
      - run: cargo miri test --lib compressor::core::hq::zopfli::tests::prefix_merge_preserves_prior_arena_entries_and_tree_ties
      - run: cargo miri test --lib compressor::core::greedy::hashers::tests
      - run: cargo miri test --lib compressor::core::fast::workspace::tests
      - run: cargo miri test --lib compressor::core::stream::tests
